# 🎥 Camera Test TMA - Тестовый прототип камеры для Telegram Mini App

Тестовый прототип для проверки возможности работы с камерой и микрофоном внутри Telegram WebView.

## 🎯 Цель проекта

Проверить технологические гипотезы:
1. ✅ `getUserMedia()` работает в Telegram WebView (iOS/Android)
2. ✅ `MediaRecorder API` записывает видео со звуком
3. ✅ Видео можно отправить на сервер через `fetch/FormData`
4. ✅ Круглый интерфейс превью (имитация Video Note)
5. ✅ PWA установка (иконка на главный экран)

## 📁 Структура проекта

```
camera-test-tma/
├── index.html              # Основной HTML
├── style.css               # Стили (круглое превью)
├── app.js                  # Логика камеры и записи
├── manifest.json           # Web App Manifest (PWA)
├── package.json            # NPM зависимости
├── vite.config.js          # Конфиг Vite
└── server/
    ├── test_server.py      # FastAPI сервер
    └── requirements.txt    # Python зависимости
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

#### Frontend (Vite)
```bash
cd camera-test-tma
npm install
```

#### Backend (Python FastAPI)
```bash
cd server
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Запуск сервера

#### Запустить FastAPI сервер
```bash
cd server
python test_server.py
```

Сервер запустится на `http://localhost:8000`

Проверить: http://localhost:8000

### 3. Запуск frontend

#### Вариант A: Локально с Vite (для разработки)
```bash
npm run dev
```

Откроется на `http://localhost:3000`

#### Вариант B: Через ngrok (для тестирования в Telegram)

1. Запустите Vite dev server:
```bash
npm run dev
```

2. В другом терминале запустите ngrok:
```bash
ngrok http 3000
```

3. Скопируйте HTTPS URL из ngrok (например: `https://xxxx.ngrok-free.app`)

4. Настройте Mini App в @BotFather с этим URL

### 4. Настройка Telegram Mini App

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newapp`
3. Выберите вашего бота
4. Название: `Camera Test`
5. Описание: `Тест камеры в Telegram`
6. Картинка: любая 640x360
7. **URL:** ваш ngrok URL или GitHub Pages URL
8. GIF: `/empty`

### 5. Деплой на GitHub Pages

#### Подготовка
```bash
npm run build
```

#### Загрузка на GitHub
```bash
git init
git add .
git commit -m "Initial commit: Camera Test TMA"
git remote add origin https://github.com/YOUR_USERNAME/camera-test-tma.git
git branch -M main
git push -u origin main
```

#### Включить GitHub Pages
1. Перейдите в Settings → Pages
2. Source: Deploy from branch
3. Branch: `main` → `/dist`
4. Save

Ваш URL: `https://YOUR_USERNAME.github.io/camera-test-tma/`

## 🎨 Функционал

### Реализовано (MVP)

✅ **Telegram WebApp интеграция**
- Полноэкранный режим
- Применение цветов темы Telegram
- Тактильная обратная связь (вибрация)

✅ **Доступ к камере и микрофону**
- Запрос разрешений через `getUserMedia`
- Обработка ошибок доступа
- Логирование всех событий

✅ **Круглое превью камеры**
- CSS `border-radius: 50%`
- Соотношение сторон 1:1
- Реальное время без задержек

✅ **Запись видео**
- 10 секунд с обратным отсчетом
- MediaRecorder API
- Формат: webm с аудио
- Ручная остановка до окончания таймера

✅ **Управление микрофоном**
- Кнопка вкл/выкл микрофона
- Визуальный индикатор состояния
- Сохранение состояния во время записи

✅ **Загрузка из галереи**
- Выбор видео из файловой системы
- Отображение имени и размера файла

✅ **Отправка на сервер**
- POST запрос с FormData
- Индикатор загрузки
- Отображение результата (размер файла)

✅ **PWA функционал**
- Web App Manifest
- Иконки 192x192 и 512x512
- Возможность добавить на главный экран

### Опциональные функции

✅ **Переключение камер**
- Кнопка смены фронтальная/тыловая
- Автоматическое определение доступных камер

✅ **Сохранение в галерею**
- Через `navigator.share()` (если поддерживается)
- Кнопка "Сохранить"

✅ **Логирование**
- Все события в консоль
- Размеры файлов
- Состояния MediaRecorder

## 🧪 Тестирование

### Обязательные проверки

- [ ] **iOS Safari в Telegram:** Доступ к камере/микрофону
- [ ] **Android Chrome в Telegram:** Доступ к камере/микрофону
- [ ] **Запись 10 секунд:** Таймер работает корректно
- [ ] **Круглое превью:** Видео отображается в круге
- [ ] **Отправка на сервер:** Файл успешно загружается
- [ ] **PWA:** Можно добавить на главный экран

### Тестовые устройства

- iPhone 12+ (iOS 15+)
- Samsung/Android 10+
- Горизонтальная ориентация

### Как тестировать

1. Откройте приложение в Telegram на телефоне
2. Разрешите доступ к камере и микрофону
3. Проверьте круглое превью
4. Запишите 10-секундное видео
5. Отправьте на сервер
6. Проверьте файл в папке `server/test_videos/`

## 📊 Результаты тестирования

### Гипотеза 1: getUserMedia в Telegram WebView
- **iOS:** ⏳ Требуется тестирование
- **Android:** ⏳ Требуется тестирование

### Гипотеза 2: MediaRecorder API
- **Качество видео:** ⏳ Требуется тестирование
- **Качество аудио:** ⏳ Требуется тестирование

### Гипотеза 3: Отправка на сервер
- **Успешность:** ⏳ Требуется тестирование
- **Размер файлов:** ⏳ Требуется тестирование

### Гипотеза 4: Круглый интерфейс
- **Визуал:** ✅ Реализовано
- **Производительность:** ⏳ Требуется тестирование

### Гипотеза 5: PWA установка
- **iOS:** ⏳ Требуется тестирование
- **Android:** ⏳ Требуется тестирование

## 🔧 Настройка сервера

### Изменить URL сервера

В файле `app.js` найдите строку:
```javascript
const serverUrl = 'http://localhost:8000/upload-test';
```

Замените на ваш URL:
```javascript
const serverUrl = 'https://your-server.com/upload-test';
```

### Деплой сервера на VPS

1. Установите Python и зависимости
2. Настройте HTTPS (Let's Encrypt)
3. Запустите сервер:
```bash
uvicorn test_server:app --host 0.0.0.0 --port 8000
```

Или с помощью systemd для автозапуска.

## 📝 API Endpoints

### POST /upload-test
Загрузка видео на сервер

**Request:**
- `video` (file): Видео файл
- `user_id` (string, optional): ID пользователя Telegram

**Response:**
```json
{
  "status": "success",
  "filename": "20240120_123456_video.webm",
  "size_bytes": 1234567,
  "size_mb": 1.18,
  "content_type": "video/webm",
  "user_id": "123456789",
  "has_audio": "likely",
  "timestamp": "2024-01-20T12:34:56",
  "saved_path": "test_videos/20240120_123456_video.webm"
}
```

### GET /videos
Список всех загруженных видео

**Response:**
```json
{
  "status": "success",
  "count": 5,
  "videos": [
    {
      "filename": "20240120_123456_video.webm",
      "size_bytes": 1234567,
      "size_mb": 1.18
    }
  ]
}
```

## 🐛 Известные проблемы

### iOS Safari
- Требуется `muted` и `playsinline` атрибуты для видео
- Автовоспроизведение может не работать без взаимодействия пользователя

### Android Chrome
- Некоторые устройства могут не поддерживать webm формат
- Fallback на другие форматы реализован

### HTTPS
- `getUserMedia` требует HTTPS
- Используйте ngrok для локального тестирования
- Для production нужен SSL сертификат

## 📚 Технологии

- **Frontend:** Vanilla JavaScript, Vite
- **Backend:** Python FastAPI
- **APIs:** getUserMedia, MediaRecorder, Telegram WebApp
- **Стили:** CSS3 (Flexbox, Grid, Animations)

## 🎯 Следующие шаги

После успешного тестирования:
1. Интегрировать в основное приложение Fitness AI Coach
2. Добавить AI-анализ видео
3. Реализовать распознавание еды
4. Добавить голосовые команды

## 📞 Поддержка

По вопросам архитектуры: @dakkkkkkkkkkkkkkkkkkkkkkkkkk

---

**Статус:** 🚧 В разработке  
**Версия:** 1.0.0  
**Дата:** Январь 2024
